// node_modules
import React from "react";
import { Box, Text } from "@chakra-ui/react";

const ClaimingPage: React.FC = (props) => {
    return (
        <Box fontSize="2xl" textAlign="center">
            <Text fontSize="6xl">Claiming Page</Text>
        </Box>
    );
};

export default ClaimingPage;
